package com.marcel.pna.usersettings.domain

import kotlinx.coroutines.test.runTest
import org.junit.Test

class UpdateUserSettingsUseCaseTest {

    @Test
    fun `When loadTrendingHeadlinesBy is Sources and headlinesSourcesIds is empty, return NoSources error`() =
        runTest { }
}